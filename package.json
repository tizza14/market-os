{
  "name": "market-os",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npm -w apps/market-data-service run dev & npm -w apps/api-gateway run dev",
    "build": "npm -ws run build",
    "test": "npm -ws run test",
    "typecheck": "npm -ws run typecheck"
  }
}
